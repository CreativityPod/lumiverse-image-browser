{
  "name": "lumiverse-image-browser",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/CreativityPod/lumiverse-image-browser.git"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "check": "npm run build && npm test && node scripts/validate-package.mjs"
  }
}
